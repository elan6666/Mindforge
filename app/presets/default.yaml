preset_mode: default
display_name: 默认助手
description: 未明确选择模式时使用的通用兜底预设。
agent_roles:
  - coordinator
execution_flow:
  - intake
  - execute
  - summarize
default_models:
  coordinator: gpt-5.4
requires_repo_analysis: false
requires_approval: false
metadata:
  stage: phase-2
  supported_domains:
    - general
    - software-engineering
    - academic-writing
