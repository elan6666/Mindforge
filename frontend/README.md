# Frontend Placeholder

Phase 1 does not implement the GUI layer yet.

Current status:
- the backend service is the primary entrypoint
- OpenHands GUI integration is intentionally deferred
- this directory exists to reserve a stable extension point for later phases

Planned later work:
- embed or extend the OpenHands Local GUI
- add preset-mode selection UI
- show task history, approvals, and result summaries

