---
phase: 05-model-routing-and-registry
plan: 01
status: completed
---

# Plan 05-01

## Objective

建立 YAML-backed provider/model registry，并暴露基础查询 API。

## Tasks

1. 定义 provider/model/routing schema
2. 实现 YAML loader 与 registry service
3. 新增 `/api/providers` 与 `/api/models`
4. 把 preset 默认模型迁移到真实 registry model id
