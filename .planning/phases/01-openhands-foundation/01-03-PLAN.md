---
phase: 01-openhands-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - app/backend/integration/openhands_adapter.py
  - app/backend/services/task_service.py
  - app/backend/core/config.py
  - app/backend/api/routes/tasks.py
  - scripts/run_local_demo.ps1
  - README.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - "本地可以启动基础服务并调用任务接口"
    - "任务接口通过适配层调用 OpenHands 或可替换执行边界"
    - "系统返回统一格式的最终结果并记录执行状态"
  artifacts:
    - path: "app/backend/integration/openhands_adapter.py"
      provides: "OpenHands adapter boundary"
      min_lines: 25
    - path: "scripts/run_local_demo.ps1"
      provides: "local demo startup script"
      min_lines: 10
    - path: "README.md"
      provides: "Phase 1 end-to-end startup and demo instructions"
      contains: "run_local_demo"
  key_links:
    - from: "app/backend/services/task_service.py"
      to: "app/backend/integration/openhands_adapter.py"
      via: "adapter invocation"
      pattern: "OpenHandsAdapter|run_task"
    - from: "scripts/run_local_demo.ps1"
      to: "app/backend/main.py"
      via: "startup command"
---

<objective>
接通 OpenHands 适配层，并形成可验证的本地端到端任务转发演示。

Purpose: 满足 Phase 1 的核心验收，让“系统入口 -> 适配层 -> OpenHands -> 规范化响应”这条链真正可运行。
Output: OpenHands adapter、任务服务接线、本地演示脚本和更新后的启动说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-openhands-foundation/01-CONTEXT.md
@.planning/phases/01-openhands-foundation/01-RESEARCH.md
@.planning/phases/01-openhands-foundation/01-01-PLAN.md
@.planning/phases/01-openhands-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement explicit OpenHands adapter boundary</name>
  <files>app/backend/integration/openhands_adapter.py, app/backend/core/config.py</files>
  <read_first>.planning/phases/01-openhands-foundation/01-CONTEXT.md, .planning/phases/01-openhands-foundation/01-RESEARCH.md, app/backend/core/config.py, app/backend/schemas/task.py</read_first>
  <action>Create `app/backend/integration/openhands_adapter.py` with an `OpenHandsAdapter` class or equivalent function boundary that accepts a normalized task request and returns a structured adapter result. Read connection/runtime values from `app/backend/core/config.py`. If direct execution cannot be completed without environment setup, implement a replaceable adapter contract with explicit TODO-safe placeholders and clear failure mapping rather than leaking raw exceptions.</action>
  <verify>rg -n "OpenHandsAdapter|run_task|openhands" app/backend/integration app/backend/core/config.py</verify>
  <acceptance_criteria>
    - app/backend/integration/openhands_adapter.py contains "OpenHandsAdapter" or an equivalent adapter type
    - app/backend/integration/openhands_adapter.py contains a task execution method
    - app/backend/core/config.py contains adapter-related settings
  </acceptance_criteria>
  <done>OpenHands 调用边界被封装在独立 integration 层。</done>
</task>

<task type="auto">
  <name>Task 2: Wire adapter into task service and route response flow</name>
  <files>app/backend/services/task_service.py, app/backend/api/routes/tasks.py</files>
  <read_first>app/backend/services/task_service.py, app/backend/services/result_normalizer.py, app/backend/integration/openhands_adapter.py, app/backend/schemas/task.py</read_first>
  <action>Update `task_service.py` so task submission goes through the adapter boundary, then through the result normalizer into `TaskResponse`. Ensure success and failure states are explicit and logged. Keep `tasks.py` route thin: it should still only call the service layer and return the normalized contract.</action>
  <verify>rg -n "OpenHandsAdapter|normalize|TaskResponse|error|success" app/backend/services app/backend/api/routes/tasks.py</verify>
  <acceptance_criteria>
    - app/backend/services/task_service.py references the OpenHands adapter
    - app/backend/services/task_service.py returns or builds TaskResponse
    - app/backend/api/routes/tasks.py does not contain direct OpenHands execution logic
  </acceptance_criteria>
  <done>任务接口和适配层已经接线，执行结果会通过统一 contract 返回。</done>
</task>

<task type="auto">
  <name>Task 3: Add local demo script and end-to-end instructions</name>
  <files>scripts/run_local_demo.ps1, README.md</files>
  <read_first>README.md, app/backend/main.py, app/backend/api/routes/tasks.py</read_first>
  <action>Create `scripts/run_local_demo.ps1` that starts the local backend in the expected way for this repository. Update `README.md` with a concrete Phase 1 demo path: install dependencies, run the script or uvicorn command, call the health endpoint, and submit a sample task request to the task endpoint. The instructions must match actual file paths and commands created by Plans 01 and 02.</action>
  <verify>rg -n "run_local_demo|uvicorn|health|tasks" scripts/run_local_demo.ps1 README.md</verify>
  <acceptance_criteria>
    - scripts/run_local_demo.ps1 exists and contains a startup command
    - README.md contains a Phase 1 demo sequence with health and task endpoint checks
    - README.md references scripts/run_local_demo.ps1
  </acceptance_criteria>
  <done>执行者可以按文档完成本地演示，验证基础任务转发链路。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "OpenHandsAdapter|run_task" app/backend/integration` returns matches
- [ ] `rg -n "TaskResponse|normalize|OpenHandsAdapter" app/backend/services` returns matches
- [ ] `README.md` and `scripts/run_local_demo.ps1` describe a runnable Phase 1 demo flow
</verification>

<success_criteria>

- All tasks completed
- OpenHands 适配边界已建立并接入任务服务
- 本地演示脚本和说明能支持 Phase 1 端到端验收
- Phase 1 满足“收到任务 -> 转发 -> 返回规范化结果”的目标
</success_criteria>

<output>
After completion, create `.planning/phases/01-openhands-foundation/01-03-SUMMARY.md`
</output>
