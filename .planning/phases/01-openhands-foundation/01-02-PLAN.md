---
phase: 01-openhands-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/backend/schemas/task.py
  - app/backend/api/routes/tasks.py
  - app/backend/services/task_service.py
  - app/backend/services/result_normalizer.py
  - app/backend/api/router.py
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - "系统存在统一任务提交接口"
    - "任务接口返回规范化响应对象"
    - "任务处理过程至少记录基础日志或状态"
  artifacts:
    - path: "app/backend/schemas/task.py"
      provides: "Task request and response models"
      min_lines: 25
    - path: "app/backend/api/routes/tasks.py"
      provides: "task submission endpoint"
      min_lines: 20
    - path: "app/backend/services/task_service.py"
      provides: "task orchestration service"
      min_lines: 25
  key_links:
    - from: "app/backend/api/routes/tasks.py"
      to: "app/backend/services/task_service.py"
      via: "service call"
      pattern: "TaskService|task_service"
    - from: "app/backend/services/task_service.py"
      to: "app/backend/schemas/task.py"
      via: "schema usage"
      pattern: "TaskRequest|TaskResponse"
---

<objective>
实现统一任务请求与响应模型，建立基础任务接口和结果标准化逻辑。

Purpose: 让你们的系统拥有独立于 OpenHands 的稳定 API contract，为后续模板、调度和上下文增强打基础。
Output: Task schema、任务提交路由、任务服务层和统一响应/日志封装。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-openhands-foundation/01-CONTEXT.md
@.planning/phases/01-openhands-foundation/01-RESEARCH.md
@.planning/phases/01-openhands-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define task request and response contracts</name>
  <files>app/backend/schemas/task.py</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-openhands-foundation/01-CONTEXT.md, app/backend/core/config.py</read_first>
  <action>Create `TaskRequest`, `TaskResponse`, and any supporting DTOs in `app/backend/schemas/task.py`. Include fields needed in Phase 1 only: `prompt`, optional `preset_mode`, optional `repo_path`, optional metadata bag, top-level `status`, normalized `output`, and optional provider/debug fields. Keep the schema neutral so later phases can extend it without breaking the contract.</action>
  <verify>rg -n "class TaskRequest|class TaskResponse|prompt|status|output" app/backend/schemas/task.py</verify>
  <acceptance_criteria>
    - app/backend/schemas/task.py contains "TaskRequest"
    - app/backend/schemas/task.py contains "TaskResponse"
    - TaskRequest includes "prompt"
    - TaskResponse includes "status" and "output"
  </acceptance_criteria>
  <done>任务接口拥有稳定、可扩展的 schema 约定。</done>
</task>

<task type="auto">
  <name>Task 2: Implement task route and service boundary</name>
  <files>app/backend/api/routes/tasks.py, app/backend/services/task_service.py, app/backend/api/router.py</files>
  <read_first>app/backend/main.py, app/backend/api/router.py, app/backend/schemas/task.py, .planning/phases/01-openhands-foundation/01-RESEARCH.md</read_first>
  <action>Add `app/backend/api/routes/tasks.py` with a POST route such as `/api/tasks`. The route must accept `TaskRequest`, call a `TaskService`, and return `TaskResponse`. Implement `app/backend/services/task_service.py` so it prepares a normalized request, delegates adapter execution in the next plan, and emits a controlled placeholder or stub response if the adapter is not yet wired. Update `app/backend/api/router.py` to register the task route.</action>
  <verify>rg -n "/api/tasks|TaskService|TaskResponse|include_router" app/backend/api app/backend/services</verify>
  <acceptance_criteria>
    - app/backend/api/routes/tasks.py contains "/api/tasks" or "tasks"
    - app/backend/api/routes/tasks.py references TaskRequest and TaskResponse
    - app/backend/services/task_service.py contains "TaskService"
    - app/backend/api/router.py includes the task router
  </acceptance_criteria>
  <done>系统具备统一任务入口，且 API 层没有直接耦合 OpenHands 细节。</done>
</task>

<task type="auto">
  <name>Task 3: Add result normalization and minimal task logging</name>
  <files>app/backend/services/result_normalizer.py, app/backend/services/task_service.py</files>
  <read_first>app/backend/schemas/task.py, app/backend/core/logging.py, .planning/phases/01-openhands-foundation/01-CONTEXT.md</read_first>
  <action>Create `app/backend/services/result_normalizer.py` to convert raw adapter output into the `TaskResponse` contract. In `task_service.py`, call the logger so each request at least records task submission, success/failure status, and adapter execution outcome. Keep persistence minimal: logging to file or standard structured logger is acceptable in Phase 1.</action>
  <verify>rg -n "normalize|logger|status|success|error" app/backend/services</verify>
  <acceptance_criteria>
    - app/backend/services/result_normalizer.py contains a normalization function or class
    - app/backend/services/task_service.py references logger setup
    - app/backend/services/task_service.py maps raw execution into TaskResponse
  </acceptance_criteria>
  <done>任务链路拥有统一返回结构和最小日志能力。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "TaskRequest|TaskResponse" app/backend/schemas/task.py` returns matches
- [ ] `rg -n "/api/tasks|TaskService" app/backend/api app/backend/services` returns matches
- [ ] Result normalization path exists and is referenced by the service layer
</verification>

<success_criteria>

- All tasks completed
- 系统存在统一任务提交接口
- 响应对象可被后续适配层和前端扩展复用
- 最小日志与结果标准化能力已经到位
</success_criteria>

<output>
After completion, create `.planning/phases/01-openhands-foundation/01-02-SUMMARY.md`
</output>
