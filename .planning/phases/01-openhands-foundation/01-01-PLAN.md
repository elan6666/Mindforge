---
phase: 01-openhands-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/main.py
  - app/backend/core/config.py
  - app/backend/core/logging.py
  - app/backend/api/router.py
  - app/backend/api/routes/health.py
  - app/backend/schemas/__init__.py
  - app/backend/services/__init__.py
  - app/backend/integration/__init__.py
  - app/backend/storage/__init__.py
  - frontend/README.md
  - pyproject.toml
  - README.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "开发者可以启动基础服务入口"
    - "项目存在清晰的后端骨架和前端扩展占位"
  artifacts:
    - path: "app/backend/main.py"
      provides: "FastAPI app factory or startup entry"
      min_lines: 20
    - path: "app/backend/core/config.py"
      provides: "central settings definition"
      min_lines: 15
    - path: "app/backend/api/routes/health.py"
      provides: "health endpoint"
      min_lines: 10
  key_links:
    - from: "app/backend/main.py"
      to: "app/backend/api/router.py"
      via: "router include"
      pattern: "include_router"
---

<objective>
建立 Multi-Agent Assistant 的基础工程骨架，形成可运行的 FastAPI 应用入口和清晰的后端分层。

Purpose: 为后续 OpenHands 适配、任务调度、模板中心和日志历史提供稳定的工程基础。
Output: 基础目录、配置与日志模块、健康检查路由、启动入口和前端扩展占位。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-openhands-foundation/01-CONTEXT.md
@.planning/phases/01-openhands-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend skeleton and app entry</name>
  <files>app/backend/main.py, app/backend/api/router.py, app/backend/api/routes/health.py, app/backend/schemas/__init__.py, app/backend/services/__init__.py, app/backend/integration/__init__.py, app/backend/storage/__init__.py</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-openhands-foundation/01-CONTEXT.md, .planning/phases/01-openhands-foundation/01-RESEARCH.md</read_first>
  <action>Create the `app/backend` package tree with `api`, `core`, `integration`, `schemas`, `services`, and `storage` packages. Add `app/backend/main.py` with a FastAPI app factory or startup entry. Add `app/backend/api/router.py` that includes a `health` router. Add `app/backend/api/routes/health.py` with a `/health` or `/api/health` endpoint returning a simple status payload. Keep Phase 1 scope strict: no template center, no agent instantiation, no repo analysis.</action>
  <verify>rg -n "FastAPI|include_router|health" app/backend</verify>
  <acceptance_criteria>
    - app/backend/main.py contains "FastAPI"
    - app/backend/main.py contains "include_router" or imports router from app/backend/api/router.py
    - app/backend/api/routes/health.py contains "health"
  </acceptance_criteria>
  <done>后端骨架存在，基础应用入口和健康检查路由可被加载。</done>
</task>

<task type="auto">
  <name>Task 2: Add central config and logging baseline</name>
  <files>app/backend/core/config.py, app/backend/core/logging.py, pyproject.toml</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-openhands-foundation/01-RESEARCH.md</read_first>
  <action>Add `app/backend/core/config.py` defining the minimal runtime settings needed in Phase 1: host, port, OpenHands connection placeholder, and log level. Add `app/backend/core/logging.py` with a shared logger setup or logging helper. Update `pyproject.toml` to declare the backend runtime dependencies needed for FastAPI startup. Do not add database tables or advanced model routing in this plan.</action>
  <verify>rg -n "host|port|openhands|log" app/backend/core pyproject.toml</verify>
  <acceptance_criteria>
    - app/backend/core/config.py contains "host" and "port"
    - app/backend/core/config.py contains an OpenHands-related setting placeholder
    - app/backend/core/logging.py contains logger setup code
    - pyproject.toml contains "fastapi"
  </acceptance_criteria>
  <done>应用具备集中配置和统一日志基础，依赖声明可支撑本地启动。</done>
</task>

<task type="auto">
  <name>Task 3: Document local skeleton usage and frontend placeholder</name>
  <files>README.md, frontend/README.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md</read_first>
  <action>Update the root `README.md` with a short Phase 1 local startup section that explains the backend entry, expected directory roles, and current scope boundary. Add `frontend/README.md` clarifying that GUI integration is placeholder-only in Phase 1 and will be expanded in later phases. Keep the docs concrete and aligned with the actual created paths.</action>
  <verify>rg -n "Phase 1|FastAPI|frontend" README.md frontend/README.md</verify>
  <acceptance_criteria>
    - README.md contains "Phase 1" and "FastAPI"
    - frontend/README.md contains "placeholder" or "占位"
  </acceptance_criteria>
  <done>项目根文档可以帮助执行者和评审理解骨架、入口和阶段边界。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "FastAPI|include_router|health" app/backend` returns matches
- [ ] `README.md` and `frontend/README.md` describe the new structure
- [ ] The created backend entry can be imported without missing package structure
</verification>

<success_criteria>

- All tasks completed
- 基础后端目录和启动入口已建立
- 健康检查路由存在并可被应用加载
- 文档明确说明了当前骨架和 Phase 1 边界
</success_criteria>

<output>
After completion, create `.planning/phases/01-openhands-foundation/01-01-SUMMARY.md`
</output>
