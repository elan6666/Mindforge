---
phase: 02-preset-template-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/schemas/preset.py
  - app/backend/services/preset_loader.py
  - app/backend/services/preset_service.py
  - app/presets/default.yaml
  - app/presets/code-engineering.yaml
  - app/presets/code-review.yaml
  - app/presets/doc-organize.yaml
  - pyproject.toml
autonomous: true
requirements:
  - PRESET-02
  - PRESET-03
user_setup: []
must_haves:
  truths:
    - "系统存在统一的 preset 模板数据结构"
    - "默认模板文件可以被后端成功加载"
    - "模板定义包含后续阶段需要消费的核心字段"
  artifacts:
    - path: "app/backend/schemas/preset.py"
      provides: "preset definition schema"
      min_lines: 25
    - path: "app/backend/services/preset_loader.py"
      provides: "preset YAML loader"
      min_lines: 20
    - path: "app/presets/default.yaml"
      provides: "default preset template"
      contains: "preset_mode:"
  key_links:
    - from: "app/backend/services/preset_loader.py"
      to: "app/backend/schemas/preset.py"
      via: "schema validation"
      pattern: "PresetDefinition"
---

<objective>
建立模板中心的基础对象和默认模板资产，形成可校验、可加载、可扩展的 preset registry 基础。

Purpose: 为后续模式选择和模板消费提供单一可信模板源。
Output: preset schema、YAML loader、preset service baseline、4 个默认模板文件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preset-template-center/02-CONTEXT.md
@.planning/phases/02-preset-template-center/02-RESEARCH.md
@app/backend/schemas/task.py
@app/backend/services/task_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define preset schemas and summaries</name>
  <files>app/backend/schemas/preset.py</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-preset-template-center/02-CONTEXT.md, app/backend/schemas/task.py</read_first>
  <action>Create `PresetDefinition` and any supporting response schema types in `app/backend/schemas/preset.py`. Include concrete fields: `preset_mode`, `display_name`, `description`, `agent_roles`, `execution_flow`, `default_models`, `requires_repo_analysis`, and `requires_approval`. Add a lighter summary type for discovery responses if needed. Keep the schema Phase-2-focused: definition only, no execution logic.</action>
  <verify>rg -n "class PresetDefinition|preset_mode|agent_roles|execution_flow|default_models" app/backend/schemas/preset.py</verify>
  <acceptance_criteria>
    - app/backend/schemas/preset.py contains "PresetDefinition"
    - PresetDefinition contains "preset_mode"
    - PresetDefinition contains "agent_roles" and "execution_flow"
    - PresetDefinition contains "default_models"
  </acceptance_criteria>
  <done>Preset schema 已稳定，后续 loader 和 API 可直接依赖。</done>
</task>

<task type="auto">
  <name>Task 2: Implement YAML loader and registry service</name>
  <files>app/backend/services/preset_loader.py, app/backend/services/preset_service.py, pyproject.toml</files>
  <read_first>app/backend/schemas/preset.py, .planning/phases/02-preset-template-center/02-RESEARCH.md, pyproject.toml</read_first>
  <action>Add YAML parsing support to `pyproject.toml` if not already present. Implement `preset_loader.py` to read preset files from a dedicated directory and validate them using `PresetDefinition`. Implement `preset_service.py` to list presets, resolve one preset by mode, return `default` when the requested mode is missing/empty, and raise a structured application error for unknown explicit modes.</action>
  <verify>rg -n "yaml|PresetDefinition|default|resolve|list_presets" app/backend/services pyproject.toml</verify>
  <acceptance_criteria>
    - pyproject.toml contains "pyyaml"
    - app/backend/services/preset_loader.py references YAML parsing
    - app/backend/services/preset_service.py contains resolve logic
    - preset_service implements default fallback and explicit unknown-mode failure
  </acceptance_criteria>
  <done>后端已有可复用的 preset registry，不再依赖硬编码模式分支。</done>
</task>

<task type="auto">
  <name>Task 3: Add default preset template files</name>
  <files>app/presets/default.yaml, app/presets/code-engineering.yaml, app/presets/code-review.yaml, app/presets/doc-organize.yaml</files>
  <read_first>.planning/phases/02-preset-template-center/02-CONTEXT.md, app/backend/schemas/preset.py</read_first>
  <action>Create four YAML preset files under `app/presets`: `default`, `code-engineering`, `code-review`, and `doc-organize`. Each file must include concrete values for `preset_mode`, `display_name`, `description`, `agent_roles`, `execution_flow`, `default_models`, `requires_repo_analysis`, and `requires_approval`. Keep values realistic but non-executing: they describe future behavior without implementing orchestration here.</action>
  <verify>rg -n "preset_mode:|agent_roles:|execution_flow:|default_models:" app/presets</verify>
  <acceptance_criteria>
    - app/presets/default.yaml exists
    - app/presets/code-engineering.yaml exists
    - every preset YAML contains "preset_mode:" and "default_models:"
    - every preset YAML contains "requires_repo_analysis:" and "requires_approval:"
  </acceptance_criteria>
  <done>系统已有可演示、可加载、可被后续阶段消费的默认模板资产。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "PresetDefinition" app/backend/schemas/preset.py` returns matches
- [ ] `rg -n "yaml|resolve|list_presets" app/backend/services` returns matches
- [ ] `rg -n "preset_mode:|default_models:" app/presets` returns matches
</verification>

<success_criteria>

- All tasks completed
- 后端存在稳定的 preset schema 和 loader
- 至少 4 个默认模板文件可以被统一注册表消费
- Phase 2 的模板定义能力已独立于具体执行链
</success_criteria>

<output>
After completion, create `.planning/phases/02-preset-template-center/02-01-SUMMARY.md`
</output>

