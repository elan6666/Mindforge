---
phase: 02-preset-template-center
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/backend/api/router.py
  - app/backend/api/routes/presets.py
  - app/backend/api/routes/tasks.py
  - app/backend/schemas/task.py
  - app/backend/services/task_service.py
  - README.md
autonomous: true
requirements:
  - PRESET-01
  - PRESET-02
  - PRESET-03
user_setup: []
must_haves:
  truths:
    - "用户可以通过 API 获取可用 preset 模式"
    - "提交任务时系统会解析并使用对应 preset"
    - "空 preset_mode 会回退 default，未知 preset_mode 会返回结构化错误"
  artifacts:
    - path: "app/backend/api/routes/presets.py"
      provides: "preset discovery endpoint"
      min_lines: 20
    - path: "app/backend/services/task_service.py"
      provides: "preset-aware task submission flow"
      min_lines: 30
    - path: "README.md"
      provides: "preset usage documentation"
      contains: "/api/presets"
  key_links:
    - from: "app/backend/api/routes/presets.py"
      to: "app/backend/services/preset_service.py"
      via: "service dependency"
      pattern: "PresetService|preset_service"
    - from: "app/backend/services/task_service.py"
      to: "app/backend/services/preset_service.py"
      via: "preset resolution before adapter execution"
      pattern: "resolve"
---

<objective>
把模板中心接入现有 API，使 preset 模式可查询、可选择，并真正影响任务提交链路。

Purpose: 让 Phase 2 从“模板资产存在”推进到“模板入口可用且被任务链消费”。
Output: preset discovery 路由、preset-aware task service、更新后的 task schema/response 元信息、文档说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preset-template-center/02-CONTEXT.md
@.planning/phases/02-preset-template-center/02-RESEARCH.md
@.planning/phases/02-preset-template-center/02-01-PLAN.md
@app/backend/schemas/task.py
@app/backend/services/task_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preset discovery API</name>
  <files>app/backend/api/routes/presets.py, app/backend/api/router.py</files>
  <read_first>app/backend/api/router.py, app/backend/schemas/preset.py, app/backend/services/preset_service.py</read_first>
  <action>Create `app/backend/api/routes/presets.py` with a lightweight `GET /api/presets` endpoint that returns available preset summaries from the preset service. Register this route in `app/backend/api/router.py`. The response should expose enough fields for later GUI/CLI integration: `preset_mode`, `display_name`, `description`, and the two boolean flags.</action>
  <verify>rg -n "GET|presets|display_name|requires_repo_analysis|requires_approval" app/backend/api</verify>
  <acceptance_criteria>
    - app/backend/api/routes/presets.py exists
    - app/backend/api/router.py includes the presets router
    - discovery endpoint returns preset summary fields, not raw YAML blobs
  </acceptance_criteria>
  <done>API 层已具备预设模式发现入口。</done>
</task>

<task type="auto">
  <name>Task 2: Make task submission preset-aware</name>
  <files>app/backend/services/task_service.py, app/backend/schemas/task.py, app/backend/api/routes/tasks.py</files>
  <read_first>app/backend/services/task_service.py, app/backend/schemas/task.py, app/backend/services/preset_service.py, app/backend/integration/openhands_adapter.py</read_first>
  <action>Update `task_service.py` so every task submission resolves a preset before calling the adapter. Empty or missing `preset_mode` must fall back to `default` and record the resolved preset in task metadata. Explicit unknown `preset_mode` values must return a structured application error mapped to the API response instead of silently falling back. Extend `TaskResponse` metadata or related schema so the caller can see which preset was resolved.</action>
  <verify>rg -n "preset|default|resolve|error|metadata" app/backend/services/task_service.py app/backend/schemas/task.py app/backend/api/routes/tasks.py</verify>
  <acceptance_criteria>
    - task_service resolves preset data before adapter invocation
    - unknown explicit preset values produce a structured failure path
    - successful responses include resolved preset information
  </acceptance_criteria>
  <done>任务提交链路已经真正消费 preset 模板，而不是只接受字符串参数。</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 2 preset usage and checks</name>
  <files>README.md</files>
  <read_first>README.md, app/backend/api/routes/presets.py, app/backend/api/routes/tasks.py</read_first>
  <action>Update `README.md` so Phase 2 users know how to list presets with `/api/presets`, how to call `/api/tasks` with `preset_mode`, what happens when `preset_mode` is omitted, and what happens when an unknown mode is supplied. Keep the examples aligned with actual endpoint names and the four default presets from Plan 01.</action>
  <verify>rg -n "/api/presets|preset_mode|default|code-engineering|code-review|doc-organize" README.md</verify>
  <acceptance_criteria>
    - README.md contains `/api/presets`
    - README.md explains missing preset fallback
    - README.md explains unknown preset failure behavior
  </acceptance_criteria>
  <done>Phase 2 的模板入口和行为已经可被演示和复现。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `GET /api/presets` is implemented and registered
- [ ] `TaskService` resolves presets and surfaces resolved metadata
- [ ] README documents preset listing and task submission behavior
</verification>

<success_criteria>

- All tasks completed
- 模板中心已通过 API 暴露和消费
- `preset_mode` 真正驱动模板解析
- Phase 2 目标在不引入多 Agent 执行逻辑的前提下完成
</success_criteria>

<output>
After completion, create `.planning/phases/02-preset-template-center/02-02-SUMMARY.md`
</output>
