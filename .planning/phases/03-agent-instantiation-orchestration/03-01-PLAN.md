---
phase: 03-agent-instantiation-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/schemas/orchestration.py
  - app/backend/services/orchestration_service.py
  - app/backend/services/task_service.py
  - app/backend/integration/openhands_adapter.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - "code-engineering 模式会自动实例化四个固定角色"
    - "角色按串行顺序执行，并共享阶段上下文"
  artifacts:
    - path: "app/backend/services/orchestration_service.py"
      provides: "serial orchestration implementation"
      min_lines: 80
    - path: "app/backend/schemas/orchestration.py"
      provides: "stage execution schema"
      min_lines: 20
  key_links:
    - from: "app/backend/services/task_service.py"
      to: "app/backend/services/orchestration_service.py"
      via: "preset-based branch"
      pattern: "code-engineering"
---

<objective>
实现 code-engineering 模式下的角色实例化和串行编排服务。
Output: 编排 schema、阶段定义、统一 stage context、失败即终止的执行路径。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add orchestration schemas and serial orchestration service</name>
  <files>app/backend/schemas/orchestration.py, app/backend/services/orchestration_service.py</files>
  <action>新增阶段执行 schema，并实现一个只负责 code-engineering 的串行编排服务。该服务需要按项目经理、后端、前端、审查的顺序执行 adapter 调用，记录每个阶段的摘要和输出。</action>
</task>

<task type="auto">
  <name>Task 2: Connect orchestration to task service</name>
  <files>app/backend/services/task_service.py</files>
  <action>让 TaskService 在 preset 为 code-engineering 时走多阶段编排路径，在其他模式下继续保留单次调用逻辑。</action>
</task>

<task type="auto">
  <name>Task 3: Improve mock adapter observability</name>
  <files>app/backend/integration/openhands_adapter.py</files>
  <action>增强 mock adapter，让阶段角色和阶段 ID 能出现在本地演示输出与 metadata 中，方便验证串行链路。</action>
</task>

</tasks>
